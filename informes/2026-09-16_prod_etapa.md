# Informe de Etapa Actual – Panadería Don Corrientes

| **Aspecto** | **Detalle** |
|-------------|-------------|
| **Qué se hizo** | • Creada la estructura base del proyecto (`src/`, `docs/`, `uploads/`). <br>• Generada la documentación completa (`specs.md`, `referencia.md`, `guia‑diseno.md`, `auditoria.md`, `README.md`). <br>• Implementado un `src/index.html` preliminar con: <br>  - Meta‑tags SEO (title, description, Open Graph, canonical, robots). <br>  - Schema.org JSON‑LD de tipo `Bakery`. <br>  - Variables CSS de la paleta y tipografía. |
| **Estado actual** | • La página está estática y funcional en modo **mobile‑first**. <br>• Falta completar el contenido (hero, productos, sección “Cómo pedir”, mapa, footer). <br>• No se ha añadido el formulario de suscripción ni el botón de delivery. <br>• No se han aplicado estilos completos (grid, responsive, accesibilidad). <br>• No se ha ejecutado ninguna auditoría de Lighthouse ni pruebas de carga. |
| **Próximos pasos** | 1. **Completar el markup**: hero, secciones, CTA, formulario de newsletter. <br>2. **Añadir CSS**: grid de productos, media queries, foco visible, contraste. <br>3. **Implementar funcionalidad**: botón de WhatsApp con número dinámico (`WHATSAPP_NUMBER`). <br>4. **Realizar auditorías**: Lighthouse (performance, SEO, accesibilidad) y pruebas de carga (RNF‑01). <br>5. **Validar accesibilidad**: contraste ≥ 4.5:1, etiquetas ARIA, orden de encabezados. <br>6. **Revisar vulnerabilidades XSS** en el formulario de suscripción. |
| **Riesgos detectados** | • **XSS**: El formulario de suscripción no tiene sanitización ni `novalidate`. <br>• **Accesibilidad**: Falta asegurar contraste y foco visible en todos los elementos interactivos. <br>• **SEO**: No se ha añadido la etiqueta `twitter:card` ni se ha verificado la presencia de todas las meta‑tags requeridas. <br>• **Performance**: Imágenes sin optimización (WebP + JPEG fallback) y ausencia de lazy‑loading. <br>• **Cumplimiento**: Posible incumplimiento de RNF‑02 (compatibilidad con navegadores) si no se prueban en versiones antiguas. <br>• **Desviaciones**: El HTML parcial no cubre los requisitos funcionales RF‑01 a RF‑07. |

> **Nota**: Se recomienda ejecutar la auditoría de seguridad y accesibilidad antes de avanzar a la fase de pruebas de usuario.